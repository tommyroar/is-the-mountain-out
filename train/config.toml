webcam_sources = [
    0, # Local camera index
    "https://example.com/stream" # Stream URL
]

metar_station = "KSEA" # Seattle-Tacoma International Airport

schedule_seconds = 1800 # 30 minutes

[lora_settings]
rank = 8
alpha = 16
target_modules = ["fc1", "fc2"] # ConvNeXt Linear layers in MLP
